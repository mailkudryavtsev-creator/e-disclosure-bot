meta:
  environment: node
  toolchain:
    name: npm
    version: 20
build:
  commands:
    - npm install
    - npx playwright install chromium --with-deps
run:
  command: node server.js
  containerPort: 3000
